<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.bcc.cyost">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

    <activity android:name=".AMainMenu" />
    <activity android:name=".AStoryList" />
    <activity android:name=".AChapterList" />
    <activity android:name=".AChapterDetails" />
    <activity android:name=".AChapterReader">
        <intent-filter >
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity android:name=".ACharacterList"></activity>
    <activity android:name=".AConseqDetail" />
    <activity android:name=".ATriggerDetail" />


    </application>
</manifest>
